const title = "the title is good"
const author = "Me"
const like = 100

// concat way
// let theResult = 'the head ' + title + 'by' + author + ' has ' + likes + ' likes'
let theResult = `the head ${title} by ${author} has ${like} likes`

console.log(theResult)
  